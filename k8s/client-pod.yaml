apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers:
    - name: client
      image: unsivilaudio/multi-client-04-29
      ports:
        - containerPort: 3000
